{
  "private": true,
  "scripts": {
    "dev": "pnpm --filter gitpeek dev",
    "build": "pnpm --filter gitpeek build",
    "test": "pnpm --filter gitpeek test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  }
}
