---
"mossy": minor
---

Add configurable Ghostty command setting. Users can now choose which command runs by default when opening Ghostty from a worktree (e.g. `copilot`, `claude`). Configurable in Settings → Editor.
