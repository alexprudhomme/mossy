---
"mossy": patch
---

Fix "check for updates" button: corrected RPC method name from `system:checkForUpdates` to `app:checkForUpdates` and fixed response property access from `result.available` to `result.updateAvailable`.
