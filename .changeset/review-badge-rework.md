---
"mossy": patch
---

Redesign worktree card status badges: Jira label now shows only the ticket key in blue, review badge only appears when PR is approved, and badge ordering is ReviewBadge (left) → PR → Conflicts → Dirty → Jira (right).
