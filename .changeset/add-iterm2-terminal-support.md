---
"mossy": minor
---

Add iTerm2 terminal support with a terminal switcher in settings. Users can now choose between Ghostty, iTerm2, and Terminal.app as their default terminal. The terminal button in worktree cards dynamically reflects the selected terminal with its own icon.
