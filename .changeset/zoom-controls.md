---
"mossy": minor
---

Add CMD+/CMD- zoom controls to scale the UI bigger or smaller. Zoom level persists across sessions. CMD+0 resets to default. Also adds a View menu with Zoom In, Zoom Out, and Actual Size items.
