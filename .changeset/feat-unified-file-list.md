---
"mossy": patch
---

Remove the staged/unstaged split in the diff panel file list. All changed files now appear in a single unified list; the checkbox is the sole indicator of whether a file is staged. Checking/unchecking still stages or unstages the file but files no longer jump between sections.
